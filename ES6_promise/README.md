# ES6 Promises

### 🧠 What I Learned
* **Promise Fundamentals:** Core mechanics of `resolve`, `reject`, and the 3 states (`pending`, `fulfilled`, `rejected`).
* **Asynchronous Control Flow:** Chaining with `.then()`/`.catch()` and writing clean code with `async/await`.
* **Error Handling:** Robust error management using `try/catch` blocks and `throw new Error()`.
* **Promise Methods:** Handling concurrent tasks with `Promise.all()` and race conditions with `Promise.race()`.
* **ES6+ Standards:** Arrow functions, ES6 modules (`import`/`export`), and object destructuring.

--- 

### 🛠️ What I Did
* Implemented **10 asynchronous JavaScript tasks** (`0-promise.js` through `9-try.js`).
* Ensured full code compliance with **ESLint (Airbnb Style Guide)**.
* Validated functionality using **Jest** unit tests on **Node.js 20**.
* Maintained a clean Git history with task-specific commit messages.